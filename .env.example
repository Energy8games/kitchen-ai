# ============================================
# Kitchen.AI — Docker Compose Configuration
# ============================================
# Copy this file to .env and fill in values:
#   cp .env.example .env

# ---------- Domain & SSL ----------
DOMAIN=kitchen-ai.example.com
ACME_EMAIL=admin@example.com

# ---------- Google AI (required) ----------
GEMINI_API_KEY=your-gemini-api-key-here

# Deprecated fallback (use GEMINI_API_KEY instead)
# GOOGLE_API_KEY=

# ---------- CORS (backend) ----------
# In production with Traefik, frontend and API share the same domain,
# so CORS is not strictly needed. Set this if you access the API
# from other origins.
CORS_ORIGIN=https://kitchen-ai.example.com

# ---------- Frontend (build-time) ----------
# When using Traefik (same domain for frontend & API), leave empty
# so the browser calls /api/* on the same host.
# For local dev without Docker, set to http://localhost:5050
VITE_API_BASE_URL=
